Main Menu:
1. Add Items
2. Edit Item
3. Remove Item
4. View Inventory
5. Help
6. Exit

Select option:


#include "stdafx.h"
#include <iostream>
#include <string>
using namespace std;

int main()
{
	int input = 0; // User-input number to select the menu item
	// Loop that repeats the menu and options until the user enters -1 (quit)
	
	while ( input != -1 )
    	{
        	cout << "Enter menu number: ";
	//Display menu
		Cout<< "1. Add"<<;
		cout<< "2. Edit"<<;
		cout<< "3. Remove"<<;
		cout<< "4. View"<<;
		cout<< "5. Help"<<;
		cout<< "6. Exit"<<;
		  if (!(cin >> input)) // User enters the Number
		  {
			  cin.clear();
			  while (cin.get()!= '\n');
			  cout << "Invalid Input!\n";
			  continue;
		  }//end if
          
	while (loopChoice !=0)
	{
		choice = selection.displayChoices();

          switch ( choice )
          {         
               		
                case 1: 					
					add(); break;// Call ADD function, then break the switch

                case 2: 
					edit(); break;// Call EDIT function, then break the switch
                    	
                case 3: 		
					remove(); break;// Call REMOVE ITEM function, then break the switch

		case 4: 	
					display(); break;// Call DISPLAY function, then break the switch
		    	
                case 5: 		
					help(); break;// Call  HELP function, then break the switch
 			
		case 6: 
					exit(); break;//exit program
			
                 default: 
					 cout << "Invalid number\n"; break;// Default to invalid number error statement
          }//end switch         
    }  // End while 
	return 0;
}//end main


